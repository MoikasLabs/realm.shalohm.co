import { WorldCanvas } from '@/components/realm/WorldCanvas';

export default function WorldPage() {
  return (
    <main className="w-full h-screen overflow-hidden">
      <WorldCanvas />
    </main>
  );
}
